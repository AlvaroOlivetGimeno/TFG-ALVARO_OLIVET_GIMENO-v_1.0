using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActiveHabilityScript : MonoBehaviour
{
    // Start is called before the first frame update
    [Header("HABILITY TYPE:")]
    public float habilityType;

    [Header("STATE TYPE:")]
    public float stateType;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
