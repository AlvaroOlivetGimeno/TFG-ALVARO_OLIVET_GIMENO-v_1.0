using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpecialHabilityScript : MonoBehaviour
{
    // Start is called before the first frame update
    [Header("HABILITY TYPE:")]
    public float speciaStateType; //1.inbulnerabilitat 2.Tir Quadruple 3.TotalParry 4.Depredador 5.SuperKill

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
